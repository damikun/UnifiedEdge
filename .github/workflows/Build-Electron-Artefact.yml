# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubActions (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_Build-Electron-Artefact --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: Build-Electron-Artefact

on: workflow_dispatch

jobs:
  windows-latest:
    name: windows-latest
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-dotnet@v3
        with:
          dotnet-version: "7.0.x"
      - name: Cache .nuke/temp, ~/.nuget/packages
        uses: actions/cache@v2
        with:
          path: |
            .nuke/temp
            ~/.nuget/packages
          key: ${{ runner.os }}-${{ hashFiles('**/global.json', '**/*.csproj') }}
        env:
          CI: false
      - name: Run './build.cmd Release_Electron Print_Net_SDK'
        run: ./build.cmd Release_Electron Print_Net_SDK
        env:
          workflow_dispatch: ${{ github.event.inputs.workflow_dispatch }}
          CI: false
      - uses: actions/upload-artifact@v3
        with:
          name: Artifacts
          path: Src/Portal/API/Artifacts/*.exe
